import { Button } from "./ui/button";

interface RedirectButtonProps {
	location: string;
	text: string;
}

// text: string, location: stringconst RedirectButton = (props: RedirectButtonProps) => {

// 	// return <div className="w-full h-full text-red-500 flex items-center justify-center text-bold">Omlouváme se, ale během generování odpovědi nastala chyba: {error}</div>;
// 	<button onClick={() => (window.location.href = props.location)}>{props.text}</button>;
// };

// export default RedirectButton;
